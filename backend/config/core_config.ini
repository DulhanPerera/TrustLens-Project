; Main file paths for models and logs.
[MODEL_PATHS]
mlp_model_path = mlp_model.keras
ae_model_path = autoencoder.keras
scaler_path = scaler.joblib
calibrator_path = isotonic_calibrator.joblib
thresholds_path = thresholds.json
; Old file names we still check if the main file is missing.
alt_mlp_paths = mlp_best.keras, trustlens_model.keras
alt_ae_paths = ae_best.keras
alt_scaler_paths = preprocess_artifacts.pkl

[LOGGER_PATHS]
; App logs go here by default.
trustlens_audit = logs/trustlens_audit.log
